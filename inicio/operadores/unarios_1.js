let a = 1;

a = a + 1; // a + b - infix (operador no meio dos dois operandos) - binário
a += 1;
//unarios
a++; // a++ - postfix (operador depois do operando) - unário
++a; // ++a - prefix (operador antes do operando) - unário

a--;
--a;

console.log(a);